// TODO: Approval queue table
// Sub-components:
//   manager/ApprovalQueue.jsx   → expenses pending this manager's approval
//   manager/ExpenseDetail.jsx   → detail modal with approve/reject + comment
// Use processApproval() from lib/approvalEngine.js on action

export default function ManagerPage() {
  return <div className="p-8">ManagerPage — TODO</div>
}
