// TODO: Build login form
// On submit → call signIn() from AuthContext
// On success → RoleRouter in App.jsx redirects based on role

export default function LoginPage() {
  return <div className="p-8">LoginPage — TODO</div>
}
