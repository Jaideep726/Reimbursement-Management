// TODO: Tabs → User Management | Approval Rules | All Expenses
// Sub-components:
//   admin/UserManagement.jsx   → create users, assign roles, set manager
//   admin/ApprovalRules.jsx    → create rules, add approvers, set sequential + pct
//   admin/AllExpenses.jsx      → view all expenses across company, override

export default function AdminPage() {
  return <div className="p-8">AdminPage — TODO</div>
}
