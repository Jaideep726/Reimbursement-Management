// TODO: Build signup form with country dropdown (fetchCountriesWithCurrencies)
// On submit → call signUp() from AuthContext
// Fields: name, email, password, country (dropdown), currency (auto-filled)

export default function SignupPage() {
  return <div className="p-8">SignupPage — TODO</div>
}
